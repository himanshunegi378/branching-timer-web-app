import { renderHook } from "@testing-library/react-hooks"
import { act } from "react-test-renderer"
import useTimerGroup from "../../hooks/useTimerGroup"
describe("useTimerGroup", () => {
    test("test", () => {
        expect(2).toBe(2)
        // const { result, wait, rerender } = renderHook(
        //     () => useTimerGroup()
        // );
    })
})
